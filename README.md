# AutoDuo

Userscript для автоматического решения математических заданий в Duolingo Math.

## Возможности

Скрипт автоматически решает следующие типы заданий:

| Тип задания | Описание |
|-------------|----------|
| **Pattern Table** | Таблицы с числовыми паттернами |
| **Equation with Blank** | Уравнения с пропусками (`\duoblank`) |
| **Comparison Choice** | Сравнения с выбором дроби (`1/4 > ?` → выбрать `1/5`) |
| **Select Operator** | Выбор оператора сравнения (`3/6 ? 1/6` → выбрать `>`) |
| **Type Inequality Answer** | Ввод дроби для неравенства (`5/5 > ?` → ввести `4/5`) |
| **Select Equivalent Fraction** | "Show this another way" — выбор эквивалентной дроби |
| **Interactive Spinner** | Круговые диаграммы для выбора дробей (включая неравенства: `5/4 > ?`) |
| **Interactive Slider** | Числовые линии со слайдером (NumberLine), включая округление чисел |
| **Expression Build** | Построение математических выражений |
| **Match Pairs** | Сопоставление пар (включая "Nearest 10/100/1000" — округление до ближайших значений) |
| **Round to Nearest** | Округление чисел до ближайших 10/100/1000 (выбор блок-диаграммы или ввод ответа) |
| **Pie Chart Input** | Ввод значений для круговых диаграмм |
| **Multiple Choice** | Вопросы с выбором ответа |
| **Tap Tokens** | Выбор токенов касанием |
| **Factor Tree** | Разложение чисел на множители |

## Установка

### Через расширение для userscripts

1. Установите одно из расширений:
   - [Tampermonkey](https://www.tampermonkey.net/) (Chrome, Firefox, Safari, Edge)
   - [Violentmonkey](https://violentmonkey.github.io/) (Chrome, Firefox)
   - [Greasemonkey](https://www.greasespot.net/) (Firefox)

2. Создайте новый скрипт и скопируйте содержимое `script.js`

3. Добавьте заголовок userscript в начало файла:

```javascript
// ==UserScript==
// @name         AutoDuo
// @namespace    https://github.com/dboyarinkov/autoduo
// @version      1.0.0
// @description  Auto-solve Duolingo Math challenges
// @author       dboyarinkov
// @match        https://www.duolingo.com/*
// @grant        none
// ==/UserScript==
```

### Через консоль браузера

1. Откройте Duolingo Math в браузере
2. Откройте Developer Tools (F12)
3. Вставьте содержимое `script.js` в консоль
4. Нажмите Enter

## Использование

После запуска скрипта в правом нижнем углу появится панель управления:

- **AUTO** — включить/выключить автоматический режим
- **SOLVE** — решить текущее задание вручную
- **SKIP** — пропустить задание
- **Clear** — очистить лог
- **_** — свернуть/развернуть панель

### Горячие клавиши

Скрипт автоматически нажимает кнопки "CHECK" и "CONTINUE" после решения.

## Логирование

Панель отображает подробный лог работы скрипта с цветовой индикацией:
- 🟢 Зелёный — успешные операции
- 🔵 Синий — информационные сообщения
- 🟡 Жёлтый — предупреждения
- 🔴 Красный — ошибки

## Архитектура

```
script.js
├── LogPanel          # Панель логирования
├── ControlPanel      # Панель управления
├── Challenge Solvers # Решатели для каждого типа заданий
│   ├── solveMathChallengeBlob
│   ├── solveMathComparisonChoice
│   ├── solveMathSelectOperator
│   ├── solveInequalityWithBlank
│   ├── solveMathSelectEquivalentFraction
│   ├── solveMathInteractiveSpinner
│   ├── solveMathInteractiveSlider
│   ├── solveMathExpressionBuild
│   ├── solveMathMatchPairs
│   ├── solveFactorTree
│   └── ...
└── Main Loop         # Основной цикл автоматизации
```

## Разработка

```bash
# Клонировать репозиторий
git clone https://github.com/dboyarinkov/autoduo.git
cd autoduo

# Внести изменения в script.js
# Тестировать через консоль браузера
```

## Ограничения

- Работает только с Duolingo Math
- Некоторые новые типы заданий могут не поддерживаться
- `console.log` не работает на Duolingo, поэтому используется кастомная панель логов

## Лицензия

[MIT](LICENSE)

## Дисклеймер

Этот проект создан в образовательных целях. Используйте на свой страх и риск. Автор не несёт ответственности за возможные последствия использования скрипта.
